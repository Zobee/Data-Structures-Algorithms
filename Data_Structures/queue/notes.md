# Queue

Basically the same as a stack, but with a first in, first out input-output method (FIFO).

Big O is O(1) for insertion and deletion (for the class-based implementation). Same with stacks, queues aren't really utilized if searching or indexing is what you're looking for